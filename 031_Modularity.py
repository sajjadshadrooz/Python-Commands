# Modularity is packages that biult and we can use them in our projects.
# We also can make packages in future.
# when we want to use a package , we should import it defore use.

#################################################################

# How to importing packages and modules.

# import random
# import random as r    -> now we can use r instead random in our code.
# from random import *  -> import all methods,functions,etc from random modules
# from random import randint , choice ,...   ->  when we need special method,fuctions, etc.
# from random import randint as newName1 , choice as newName2 ,... -> when we need special method,fuctions, etc with special name.

#################################################################

